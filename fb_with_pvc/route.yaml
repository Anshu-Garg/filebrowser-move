apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: filebrowser-isf
  name: filebrowser-isf
  namespace: file-browser
spec:
  port:
    targetPort: 80-tcp
  to:
    kind: Service
    name: filebrowser-isf
